<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>
    View Class - <%= course.courseName %> - <%= subject.subjectName %> - Chapter <%= chapter.chapterNumber %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .video-container {
      max-width: 800px;
      margin: auto;
    }
  </style>
</head>
<body>
  <div class="container my-4">
    <h1 class="mb-3"><%= course.courseName %> - <%= subject.subjectName %></h1>
    <h2 class="mb-4">Chapter <%= chapter.chapterNumber %>: <%= chapter.chapterName %></h2>

    <% if (chapter.screenRecordingLink) { %>
      <div class="video-container mb-4">
        <video controls class="w-100">
          <source src="<%= chapter.screenRecordingLink %>" type="video/webm">
          Your browser does not support the video tag.
        </video>
      </div>
      <div class="mb-4">
        <a href="<%= chapter.screenRecordingLink %>" class="btn btn-primary" download>Download Video</a>
      </div>
    <% } else { %>
      <div class="alert alert-warning" role="alert">
        No class recording available.
      </div>
    <% } %>

    <% if (typeof transcript !== 'undefined' && transcript) { %>
      <div class="card mb-4">
        <div class="card-header">
          Class Transcript
        </div>
        <div class="card-body">
          <p class="card-text"><%= transcript %></p>
        </div>
      </div>
    <% } else { %>
      <div class="alert alert-info" role="alert">
        Transcript not available. To generate a transcript, you could integrate a speech-to-text service (e.g., Google Cloud Speech-to-Text, AWS Transcribe, or others) which extracts audio from the video and converts it into text.
      </div>
    <% } %>

    <a href="/courses/dashboard" class="btn btn-secondary">Back to Dashboard</a>
  </div>

</body>
</html>
